# Enterprise-AI-Knowledge-Agent-Chat-
Multi-tenant AI chat platform delivering real-time answers over structured docs. Uses RAG with vector search and a local LLM for context-aware responses. Features JWT auth, RBAC, idempotent endpoints, async query handling, and Dockerized, production-ready deployment.
